#include <bits/stdc++.h>

using ll = long long;

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    char s[100];
    scanf("%s", s);
    printf("%s", s);

    return 0;
}